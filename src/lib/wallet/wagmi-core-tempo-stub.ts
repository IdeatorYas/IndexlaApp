/** Stub — @wagmi/core@2.22.1 has no ./tempo export; AppKit nested connectors@8 imports it. */
export function tempoWallet() {
  throw new Error("tempoWallet is not available in this IndexLa build");
}
